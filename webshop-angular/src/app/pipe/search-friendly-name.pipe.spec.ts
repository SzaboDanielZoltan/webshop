import { SearchFriendlyNamePipe } from './search-friendly-name.pipe';

describe('SearchFriendlyNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFriendlyNamePipe();
    expect(pipe).toBeTruthy();
  });
});
