import { UrlPostfixValidatePipe } from './url-postfix-validate.pipe';

describe('UrlPostfixValidatePipe', () => {
  it('create an instance', () => {
    const pipe = new UrlPostfixValidatePipe();
    expect(pipe).toBeTruthy();
  });
});
